<!DOCTYPE html>
<html>

<head>
    <title>글 수정</title>
    <%- include('header') %>
</head>

<body>
    <div class="container mt-5 mb-5" style="max-width: 800px;">
        <div class="modern-card">

            <h2 class="fw-bold mb-4">🛠️ 글 수정하기</h2>

            <form action="/edit/<%= post.id %>" method="post">
                <div class="mb-4">
                    <label class="form-label text-secondary fw-bold small">제목</label>
                    <input type="text" name="title" class="form-control form-control-lg" value="<%= post.title %>"
                        required>
                </div>

                <div class="form-check mb-4">
                    <input class="form-check-input" type="checkbox" name="is_notice_chk" id="noticeCheck"
                        <%=post.is_notice ? 'checked' : '' %> style="cursor: pointer;">
                    <label class="form-check-label fw-bold text-danger" for="noticeCheck" style="cursor: pointer;">
                        📌 공지사항으로 등록
                    </label>
                </div>

                <div class="mb-4">
                    <label class="form-label text-secondary fw-bold small">내용</label>
                    <textarea name="content" class="form-control" rows="12" required
                        style="resize: none;"><%= post.content %></textarea>
                </div>

                <div class="d-flex justify-content-end gap-2 border-top pt-4">
                    <button type="button" onclick="history.back()"
                        class="btn btn-light-custom text-secondary">취소</button>
                    <button type="submit" class="btn btn-primary px-4">수정 완료</button>
                </div>
            </form>

        </div>
    </div>
</body>

</html>