<!DOCTYPE html>
<html>

<head>
    <title>
        <%= post.title %>
    </title>
    <%- include('header') %>
</head>

<body>
    <div class="container mt-5 mb-5" style="max-width: 800px;">

        <div class="modern-card">
            <div class="border-bottom pb-4 mb-4">
                <% if(post.is_notice) { %>
                    <span class="badge-notice mb-2 d-inline-block">공지사항</span>
                    <% } %>
                        <h1 class="fw-bold mb-3" style="font-size: 2rem; letter-spacing: -0.5px;">
                            <%= post.title %>
                        </h1>
                        <div class="d-flex text-secondary small">
                            <span class="me-3">작성일: <%= new Date(post.created_at).toLocaleDateString() %></span>
                            <span>조회수: <%= post.views %></span>
                        </div>
            </div>

            <div class="py-2 mb-5"
                style="min-height: 300px; font-size: 1.1rem; color: #374151; white-space: pre-wrap; line-height: 1.8;">
                <%= post.content %>
            </div>

            <div class="d-flex justify-content-between pt-4 border-top">
                <a href="/" class="btn btn-light-custom text-secondary px-4">목록으로</a>
                <div class="d-flex gap-2">
                    <a href="/edit/<%= post.id %>" class="btn btn-light-custom text-primary fw-bold">수정</a>
                    <a href="/delete/<%= post.id %>" class="btn btn-light-custom text-danger fw-bold"
                        onclick="return confirm('정말 삭제하시겠습니까?');">삭제</a>
                </div>
            </div>
        </div>
    </div>
</body>

</html>